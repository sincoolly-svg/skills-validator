{
  "name": "@sincoolly-svg/skills-validator",
  "version": "0.1.0",
  "description": "Static safety checks for AI agent skill repositories.",
  "main": "dist/index.js",
  "bin": {
    "skills-validator": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "ts-node": "^10.9.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
